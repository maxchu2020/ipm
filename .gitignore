# 设备配置采集文件：含明文口令哈希、SNMP community、客户电路信息，不入库
running-config/

# 统计输出：由 running-config 生成，可随时重跑
output/

# ROA VRP 缓存：从 rpki.cloudflare.com 下载后过滤所得，可随时重取
cache/

# 邮件凭证
.env

# Python
__pycache__/
*.py[cod]
